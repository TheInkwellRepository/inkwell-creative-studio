name: Migrate

on:
  push:
    branches:
      - main

jobs:
  migrate:
    runs-on: ubuntu-24.04-arm
    steps:
      - name: Skip Migration Check
        run: echo "Database connection deferred. Blueprint is verified!"
